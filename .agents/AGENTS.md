# Smart Forms — Locked Project Rules

## 🔒 Locked Component Rules

### 1. Locked MCQ Answering View (`FormAnswerView.tsx`)
- **DO NOT ALTER OR REMOVE THE LOCKED MCQ ANSWERING VIEW**:
  - The MCQ Respondent Questionnaire View (`FormAnswerView.tsx`) is locked in its clean, centered 4-choice layout (**[ A ]**, **[ B ]**, **[ C ]**, **[ D ]**).
  - Keeps keyboard navigation (`Key A, B, C, D`), step progress bar, dark/light theme toggle, and smooth node branching transitions.
  - Do not re-add mode toggles or extraneous side context panels to `FormAnswerView.tsx`.
